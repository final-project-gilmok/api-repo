spring:
  datasource:
    url: jdbc:mysql://localhost:3307/gilmok_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
  rabbitmq:
    host: localhost
    port: 5672

logging:
  level:
    kr.gilmok.api: DEBUG
