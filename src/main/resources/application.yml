spring:
  application:
    name: auth-service

  datasource:
    # 1. DB ??: Docker? ?? MySQL? ??(localhost)?? ??
    url: jdbc:mysql://localhost:3307/gilmok_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    # 2. ?? ??: root ??? ??? ???? ??
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      # 3. ?? ???? ?? ???? ???? ?? ??? 'create' ??
      # ?, ??? ??? ????? 'update'? ?????.
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        # MySQL 8.0 ?? ??? ???? Dialect ?? ?? (?? ?? ??)

  # 4. Redis ??: ?? ?? ? ?? ???
  data:
    redis:
      host: localhost
      port: 6379

  # 5. RabbitMQ ??: ??? ? ???
  rabbitmq:
    host: localhost
    port: 5672

server:
  # 6. ??? ??: auth-repo? ?? 8081 ?? ?? (8080? Gateway?)
  port: 8081

# Queue admission settings
queue:
  admission-rps: 10
  default-event-id: default
  admitted-ttl-seconds: 300

logging:
  level:
    kr.gilmok.api: DEBUG
